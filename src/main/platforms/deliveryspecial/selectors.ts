export const deliverySpecialSelectors = {
  username: '#id, input[name="id"]',
  password: 'input[name="password"], input[type="password"]',
  loginButton: '#loginButton, button[type="submit"]'
} as const
